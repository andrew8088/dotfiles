[module/battery]
type = internal/battery
battery = BAT0
adapter = ADP1
full-at = 98

bg = ${colors.nord0}
fg = ${colors.nord12}
label = %{T2}%{T-} %{T1}%percentage:2%%%{T-}

format-charging = <label-charging>
label-charging = ${self.label}
label-charging-background = ${self.bg}
label-charging-foreground = ${self.fg}
label-charging-padding = ${vars.padding}

format-discharging = <label-discharging>
label-discharging = ${self.label}
label-discharging-background = ${self.bg}
label-discharging-foreground = ${self.fg}
label-discharging-padding = ${vars.padding}

format-full = <label-full>
label-full = ${self.label}
label-full-background = ${self.bg}
label-full-foreground = ${self.fg}
label-full-padding = ${vars.padding}
